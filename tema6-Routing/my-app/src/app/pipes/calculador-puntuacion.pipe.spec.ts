import { CalculadorPuntuacionPipe } from './calculador-puntuacion.pipe';

describe('CalculadorPuntuacionPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculadorPuntuacionPipe();
    expect(pipe).toBeTruthy();
  });
});
